# Calendar App Environment Variables

# Base URL for the application (used for Open Graph meta tags)
# In production, set this to your deployed URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Google Calendar Integration (Optional)
# Get your client ID from: https://console.cloud.google.com/apis/credentials
# Required scopes: calendar, userinfo.email
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here

# Microsoft Outlook Integration (Optional)
# Get your client ID from: https://portal.azure.com/
# Required scopes: Calendars.ReadWrite, User.Read
NEXT_PUBLIC_MICROSOFT_CLIENT_ID=your_microsoft_client_id_here

# Notes:
# - Create a .env.local file in the root directory with your actual values
# - Never commit .env.local to version control
# - The app works without these variables, but OAuth integrations will be disabled
